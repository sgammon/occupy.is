from project.handlers import OccupyHandler


class Offline(OccupyHandler):

	''' Handler for a (mostly) static page that is aggressively cached by HTML5. Users are sent to this page when they are offline and try to access online content. '''

	pass